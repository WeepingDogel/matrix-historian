# Web UI Dockerfile removed.

# The Streamlit web UI is no longer part of this repository. The API service
# remains and is built/packaged using the main application Dockerfile at the
# repository root. This placeholder prevents accidental usage of an obsolete
# web UI image build.

